[package]
name = "locadora2"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1"
rand = "0.8"
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }